import React from 'react';
import {Button} from 'antd'

const Example = () => {
  return (
    <div>
      Example
      <Button>12222</Button>
    </div>
  );
};

Example.propTypes = {
};

export default Example;
